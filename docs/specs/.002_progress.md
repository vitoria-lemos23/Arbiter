# 002 Create Tournament UI — Implementation Progress (WIP)

> Scratch note to resume. Spec: `docs/specs/002_create_tournament_ui.md`.
> Branch: `contract_init`. Contracts/DB/frontend committed in `e5262bc`;
> indexer + follow-ups in the working tree (uncommitted).

## ✅ Done & verified

### Contracts (task #1) — DONE, tests pass (40 passing)
- `Tournament.sol`: `uint256 public prize`; `initialize` payable, sets prize;
  `TournamentInitialized` enriched.
- `TournamentFactory.sol`: `createTournament(params, bytes32 salt)` payable,
  `Clones.cloneDeterministic`, forwards value, enriched `TournamentCreated`,
  `predictTournamentAddress(salt) view`.
- ABI regenerated; `pnpm --filter @arbiter/contracts test` → 40 passing.

### DB (task #2) — DONE
- `schema.ts`: `tournamentMetadata` (Drizzle-owned). Migration `0001`.
- `ponderTournament.ts`: read-only `pgSchema("ponder")` mapping of `tournament`.

### Frontend (task #3) — DONE, verified
- `env.ts` + `env.test.ts`: optional `NEXT_PUBLIC_FACTORY_ADDRESS`.
- `wagmi.ts`: `tournamentChainId` + `tournamentFactoryAddress`.
- Schema / hook / form / page / Header CTA / shadcn select+label — all in place.
- `lib/predictCloneAddress.ts`: pure `cloneInitCode` + `predictCloneAddress`.
- **Hook now imports the extracted lib** (inline CREATE2 dup removed).
- **`lib/predictCloneAddress.test.ts`**: asserts client derivation == on-chain
  `predictTournamentAddress` using a concrete vector captured from hardhat.
- Fixed two latent bugs found during verify:
  - `schema/createTournament.test.ts` built dates from `toISOString()` (UTC) but
    the schema parses datetime-local as LOCAL — flipped in non-UTC zones (UTC-3).
    Now uses a local-wall-clock `localDatetime()` helper.
  - `schema/createTournament.ts` used a `0n` BigInt literal (tsconfig target
    ES2017) — rewrote the `ethAmount` refine to not need it.
- `pnpm --filter @arbiter/web {test,typecheck}` → pass (29 tests).

### Indexer (task #4) — DONE, typecheck + tests pass
- `apps/indexer/` Ponder 0.16 project (new workspace, globbed by `apps/*`):
  - `package.json`: `ponder` + `@arbiter/contracts` + `viem` + `vitest`;
    scripts `dev`/`start` use `--schema ponder`; `codegen`/`typecheck`/`test`.
  - `ponder.config.ts`: chain + `TournamentFactory` (abi from @arbiter/contracts,
    address/rpc/startBlock from `INDEXER_*` env, defaults → local hardhat).
    Auto-uses `DATABASE_URL`.
  - `ponder.schema.ts`: `onchainTable("tournament", …)` with explicit snake_case
    names matching `packages/db/src/ponderTournament.ts` (hex→text,
    bigint→numeric(78,0), integer, timestamp).
  - `src/toTournamentRow.ts`: pure event→row decode (extracted for testing).
  - `src/index.ts`: `ponder.on("TournamentFactory:TournamentCreated")` →
    `context.db.insert(tournament).values(toTournamentRow(event))`.
    NOTE import is `ponder:registry` (not `"ponder"` — docs were wrong).
  - `src/api/index.ts`: REQUIRED by Ponder — default-exports a Hono app with
    `graphql` (/graphql) + `client` (/sql/*). Needs `hono` as a dep (Ponder
    peer). Without this file `ponder dev` fails at `stage=api`.
  - `test/toTournamentRow.test.ts`: fixture → row, all fields decoded, uint256
    precision preserved. MUST live OUTSIDE `src/` — Ponder loads every file
    under `src/` as an indexing function and chokes on vitest globals.
  - `.env.example`, `tsconfig.json`, `.gitignore` (ignores `.ponder`; keeps
    `ponder-env.d.ts`), generated `ponder-env.d.ts` committed.
- `turbo.json` build env: added `NEXT_PUBLIC_FACTORY_ADDRESS`.
- `apps/web/.env.example`: added `NEXT_PUBLIC_FACTORY_ADDRESS`.

### Full verify (task #5) — DONE (automated portion)
- `pnpm typecheck` → 5/5 tasks pass (incl. indexer).
- `pnpm test` → contracts (40) + web (29) + indexer (2) all pass.
- `pnpm build` → web + contracts succeed; `/tournaments/new` route builds.
- `pnpm exec biome check` on new files → clean.

## ⏭️ REMAINING

1. **Design import + 5-step wizard (spec step 8)** — DONE. Imported the TournyBox
   design via the `claude_design` MCP (`DesignSync`, project `25ec4c99…`). App
   theme already matched (Stone + Lime + Geist Mono). Per follow-up direction,
   rebuilt the create flow as a **5-step wizard** (Name → Format → Prize → Apply
   → Review):
   - `CreateTournamentWizard.tsx` (state + per-step validation + Back/Continue/
     Deploy), `wizard/Stepper.tsx`, `wizard/steps.tsx` (5 step groups),
     `wizard/fields.tsx` (Field/EthInput/ChoiceGroup/Notice). Old
     `CreateTournamentForm.tsx` deleted; `page.tsx` is now a thin shell. Added
     `components/ui/textarea.tsx`.
   - Schema gained off-chain `description` + `game`, and wizard helpers
     (`WizardValues`, `INITIAL_WIZARD_VALUES`, `WIZARD_STEPS`,
     `collectFieldErrors`, `stepFieldErrors`, `firstStepWithError`) with a new
     `schema/wizardValidation.test.ts`. Hook logs description/game too.
   - Decisions (asked): unsupported design features (payout split, ERC-20 token,
     registration fields, judge modes, cover upload) are **disabled "coming
     soon" placeholders**; judges collected as **real addresses**.
   - Spec `002_create_tournament_ui.md` updated (title, goals, non-goals, UX,
     components, business rules, plan, decision log).
   - Verified: web typecheck + build + **35 tests** pass; Biome clean.
2. **Manual local-node checklist** (spec Testing → Manual) — NOT run here
   (needs a live hardhat node + deployed factory + Ponder + a browser wallet):
   - deploy factory (Ignition), set `NEXT_PUBLIC_FACTORY_ADDRESS`;
   - `pnpm --filter @arbiter/indexer dev` (set `INDEXER_FACTORY_ADDRESS`);
   - submit a tournament → console logs predicted addr + metadata pre-sign,
     prize shows as tx value, success addr matches, a `tournament` row appears.
3. **Commit** — working tree changes (indexer + hook dedupe + test fixes +
   env/turbo) are not yet committed.

## Open decisions / watch-outs
- Ponder writes into the `ponder` Postgres schema (`--schema ponder`); the app
  reads it read-only via `@arbiter/db`'s `ponderTournament.ts`. Keep the two
  column definitions in sync.
- Ponder `t.timestamp()` is timestamp WITHOUT tz; the Drizzle mapping matches.
- `canSubmit` gates only on `NEXT_PUBLIC_FACTORY_ADDRESS`; connected/chain
  gating lives in the form render (matches the counter pattern).
