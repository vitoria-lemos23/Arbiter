# 002 Create Tournament UI — Implementation Progress (WIP)

> Scratch note to resume. Spec: `docs/specs/002_create_tournament_ui.md`.
> Branch: `contract_init`. Nothing committed yet — all changes in working tree.

## ✅ Done & verified

### Contracts (task #1) — DONE, tests pass (40 passing)
- `Tournament.sol`: added `uint256 public prize`; `initialize` is now `payable`,
  sets `prize = msg.value`; `TournamentInitialized` enriched with `prize`.
- `TournamentFactory.sol`: `createTournament(params, bytes32 salt)` is `payable`,
  uses `Clones.cloneDeterministic`, forwards `{value: msg.value}`, emits enriched
  `TournamentCreated` (format, maxPlayers, entryFee, prize, startDate, endDate).
  Added `predictTournamentAddress(bytes32 salt) view`.
- Updated `Tournament.t.sol`, `TournamentFactory.t.sol`, `test/TournamentFactory.ts`,
  `scripts/try-tournament.ts` (salt threading + prize/predict/salt-reuse tests).
- ABI regenerated: `pnpm --filter @arbiter/contracts build` (verified: ABI has
  `salt`, `payable`, enriched `TournamentCreated`, `predictTournamentAddress`).
- Verified: `pnpm --filter @arbiter/contracts test` → 40 passing.

### DB (task #2) — DONE, typecheck + generate pass
- `packages/db/src/schema.ts`: added `tournamentMetadata` table (Drizzle-owned).
- `packages/db/src/ponderTournament.ts`: NEW — read-only `pgSchema("ponder")`
  mapping of the `tournament` table (Ponder owns/writes it; kept out of
  `schema.ts` so drizzle-kit never emits DDL for it). Re-exported from `index.ts`.
- Migration generated: `packages/db/drizzle/0001_bent_grim_reaper.sql`
  (only `tournament_metadata`, as intended). `pnpm --filter @arbiter/db typecheck` OK.

### Frontend (task #3) — MOSTLY DONE, not yet type/test-verified
- `apps/web/src/env.ts`: added optional `NEXT_PUBLIC_FACTORY_ADDRESS`
  (refactored shared `optionalAddress` validator). `env.test.ts` updated.
- `apps/web/src/shared/web3/config/wagmi.ts`: exported `tournamentChainId` +
  `tournamentFactoryAddress`.
- `features/tournaments/schema/createTournament.ts`: Zod schema + `CreateTournamentValues`
  + `toTournamentParams()` + `prizeWei()` + `MAX_PLAYERS_OPTIONS` + `TOURNAMENT_FORMATS`.
  Test written: `schema/createTournament.test.ts`.
- `features/tournaments/hooks/useCreateTournament.ts`: wallet/tx logic, random salt,
  client CREATE2 prediction, console.log metadata, write `[params, salt]` + value.
- `features/tournaments/components/CreateTournamentForm.tsx`: presentational form,
  inline per-field errors, state-driven submit label, success shows predicted addr.
- `app/tournaments/new/page.tsx`: route shell rendering the form in a Card.
- `shared/layout/Header.tsx`: Create CTA now → `/tournaments/new` (was `/create`).
- shadcn `select` + `label` added to `components/ui` (via `pnpm dlx shadcn add`).
- `features/tournaments/lib/predictCloneAddress.ts`: NEW pure `cloneInitCode` +
  `predictCloneAddress` extracted for testability.

## ⏭️ NEXT STEPS (resume here)

1. **Wire the hook to the extracted lib** — update `useCreateTournament.ts` to
   import `predictCloneAddress`/`cloneInitCode` from
   `../lib/predictCloneAddress` and delete the inline copies (currently
   DUPLICATED — the hook still has its own `cloneInitCode`; dedupe it).
2. **Generate a real CREATE2 vector** and write
   `features/tournaments/lib/predictCloneAddress.test.ts` asserting the client
   derivation == on-chain `predictTournamentAddress`. Capture a concrete tuple
   `{implementation, factory, salt, predicted}` from a hardhat script
   (`network.create()` → deploy factory → `factory.read.predictTournamentAddress([salt])`)
   and hardcode it into the test.
3. **Verify frontend**: `pnpm --filter @arbiter/web test` (vitest) and
   `pnpm --filter @arbiter/web typecheck`. Watch for:
   - Radix `Select` form participation via `name`/`defaultValue` (submits hidden input).
   - viem `getContractAddress` CREATE2 param names.
4. **Indexer (task #4)** — NOT STARTED. Scaffold `apps/indexer/` Ponder project:
   - `package.json` (`ponder` dep, scripts: dev/start/db/codegen; type module),
   - `ponder.config.ts` (createConfig: chains {id, rpc}, contracts
     {TournamentFactory: abi from @arbiter/contracts, chain, address, startBlock}),
   - `ponder.schema.ts` (`onchainTable("tournament", ...)` — MUST match
     `packages/db/src/ponderTournament.ts` types: hex→text, bigint→numeric(78,0),
     integer, timestamp),
   - `src/index.ts` handler: `ponder.on("TournamentFactory:TournamentCreated")`
     → `context.db.insert(tournament).values({...})` using event.args + event.block
     (timestamp→createdAt as Date, number→blockNumber) + event.transaction.hash.
   - Point Ponder at same `DATABASE_URL`, own schema `ponder` (set schema/DATABASE_SCHEMA).
   - Add to pnpm workspace (already globbed by `apps/*`) + turbo pipeline env if needed.
   - NOTE: `pnpm install` for ponder may be heavy/offline-risky — attempt, else document.
   - Indexer test: given a `TournamentCreated` fixture, handler inserts a row with
     every field decoded (wei→numeric, unix→timestamp).
5. **Design import (spec step 8)** — SKIPPED: needs `claude_design` MCP +
   interactive `/design-login` auth not available. Built a clean shadcn form instead.
   Revisit if design fidelity required.
6. **Full verify (task #5)**: `pnpm build` across web+contracts+db+indexer;
   `pnpm test`; then the manual local-node checklist in the spec.

## Open decisions / watch-outs
- `canSubmit` is based only on `Boolean(tournamentFactoryAddress)` (matches spec
  rule 10 + the counter pattern); connected/chain gating is in the form render.
- Ponder `t.timestamp()` is timestamp WITHOUT tz (spec table says timestamptz) —
  read-only Drizzle mapping intentionally uses `timestamp(...)` w/o tz to match.
- Header CTA was reused instead of adding a duplicate item to `MainNav` (spec
  table named MainNav, but the existing Create button IS the CTA).
