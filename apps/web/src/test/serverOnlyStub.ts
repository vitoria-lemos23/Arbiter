// Test stub for the `server-only` marker package, which Next.js resolves via its
// own bundler alias (it is not a real installed dependency). Aliased in
// `vitest.config.ts` so server modules can be imported under Vitest's node env.
export {};
